{
  "version": 2,
  "builds": [
    {
      "src": "server.js", // Adjust this path to point directly to server.js
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js" // Adjust this path as well
    }
  ]
}
